# Secrets: each copy of the template keeps its own .env (see .env.example)
.env

# Dependencies and local toolchain
node_modules/
whisper.cpp/
.pixi/*
!.pixi/config.toml
.tinytex/

# Rendered output, handoff copies, and closeout bundles
out/
deliverables/
archives/

# Labs publish to their own repos
labs/

# Derived audio
*.16khz.wav

.DS_Store

# Manim renders (rebuilt by scripts/manim-render.sh), Manim's Tex cache,
# Python bytecode, and licensed music and effects (the mix sheet records each ID)
public/manim/
/media/
manim/media/
__pycache__/
public/audio/
