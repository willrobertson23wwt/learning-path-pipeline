# Dependencies and local toolchain (reinstalled per machine)
node_modules/
whisper.cpp/

# Rendered output, the copies handed to the editor, and close-out zips
out/
deliverables/
archives/

# Secrets — every team member uses their own .env (see .env.example)
.env

# Derived audio artifacts (regenerated by scripts/transcribe.mjs)
*.16khz.wav

# Labs publish to their own per-lab GitHub repos, never tracked in a course repo
labs/

.DS_Store
